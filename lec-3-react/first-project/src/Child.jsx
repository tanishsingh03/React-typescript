import React from 'react'
import {useState} from 'react'
const Child = (props) => {
let name 
  return (
    <div>
      
    </div>
  )
}

export default Child



//difference between state and props
//state are mutable
//props are immutable
//state can be changed using setState
//props cannot be changed by the component that receives them
//props are passed  data from parent to child components
//state is managed within the component
//props are immutable
//props are read-only
